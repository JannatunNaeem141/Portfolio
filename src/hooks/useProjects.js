import React, { useState } from 'react';

const useProjects = () => {
    const [projects, setProjects] = useState([]);

    return (
        <div>

        </div>
    );
};

export default useProjects;